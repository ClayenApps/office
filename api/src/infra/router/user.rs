use axum::{routing::post, Router};

use crate::AppState;

pub fn router() -> Router {
    Router::new()
        // .route("/register", post())
        // .route("/login", post())
        // .route("/logout", post())
}
